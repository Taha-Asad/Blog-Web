<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= siteName %> — Verification Code</title>
    <!-- Inline styles for best email client support -->
    <style>
      /* Basic reset for email clients */
      body,
      table,
      td,
      a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
      }
      table {
        border-collapse: collapse !important;
      }
      img {
        border: 0;
        height: auto;
        line-height: 100%;
        outline: none;
        text-decoration: none;
      }
      body {
        width: 100% !important;
        height: 100% !important;
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          "Helvetica Neue", Arial, sans-serif;
        background-color: #f4f6f8;
        color: #111827;
      }

      .container {
        max-width: 600px;
        margin: 28px auto;
        background: #ffffff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 6px 20px rgba(16, 24, 40, 0.08);
      }
      .header {
        padding: 20px;
        text-align: center;
        background: linear-gradient(90deg, #0ea5e9 0%, #2563eb 100%);
        color: white;
      }
      .logo {
        max-height: 40px;
        display: inline-block;
        margin-bottom: 6px;
      }
      .body {
        padding: 28px;
      }
      .greeting {
        font-size: 16px;
        margin-bottom: 12px;
      }
      .intro {
        color: #374151;
        font-size: 14px;
        line-height: 1.5;
        margin-bottom: 18px;
      }
      .code-box {
        display: block;
        width: fit-content;
        margin: 12px 0 18px;
        padding: 14px 20px;
        font-size: 22px;
        letter-spacing: 6px;
        font-weight: 700;
        background: #f3f4f6;
        border-radius: 8px;
        color: #111827;
      }
      .cta {
        display: inline-block;
        padding: 12px 18px;
        border-radius: 8px;
        background: #2563eb;
        color: white;
        text-decoration: none;
        font-weight: 600;
        margin-top: 8px;
      }
      .note {
        font-size: 13px;
        color: #6b7280;
        margin-top: 16px;
      }
      .footer {
        padding: 16px 20px;
        font-size: 13px;
        color: #9ca3af;
        text-align: center;
        background: #fbfbfd;
      }
      .muted {
        color: #9ca3af;
        font-size: 13px;
      }
      @media (max-width: 480px) {
        .container {
          margin: 12px;
        }
        .code-box {
          font-size: 20px;
          letter-spacing: 4px;
          padding: 12px 16px;
        }
      }
    </style>
  </head>
  <body>
    <table width="100%" cellpadding="0" cellspacing="0" role="presentation">
      <tr>
        <td align="center">
          <div class="container" role="article" aria-roledescription="email">
            <div class="header">
              <% if (logoUrl) { %>
              <img
                src="<%= logoUrl %>"
                alt="<%= siteName %> logo"
                class="logo"
              />
              <% } else { %>
              <div style="font-size: 18px; font-weight: 700">
                <%= siteName %>
              </div>
              <% } %>
            </div>

            <div class="body">
              <div class="greeting">
                Hello<% if (username) { %> <%= username %><% } %>,
              </div>

              <div class="intro">
                Use the verification code below to complete your
                <strong><%= purpose || "account verification" %></strong> on
                <strong><%= siteName %></strong>.
              </div>

              <div class="code-box" aria-label="verification code">
                <%= verificationCode %>
              </div>

              <% if (verifyUrl) { %>
              <a href="<%= verifyUrl %>" class="cta">Verify Now</a>
              <% } %>

              <p>
                This code will expire in
                <strong><%= expiresIn %></strong> (around
                <strong><%= expiresAt %></strong>).
              </p>

              <div style="margin-top: 18px" class="muted">
                If you didn't request this email, you can safely ignore it. For
                help, contact
                <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>.
              </div>
            </div>

            <div class="footer">
              © <%= new Date().getFullYear() %>
              <strong><%= siteName %></strong>. All rights reserved.
            </div>
          </div>

          <!-- Plain-text fallback visible in very old clients that strip HTML -->
          <div
            style="
              max-width: 600px;
              margin: 12px auto;
              font-family: Arial, Helvetica, sans-serif;
              color: #6b7280;
              font-size: 13px;
              text-align: left;
            "
          >
            <p style="margin: 10px 0 0">
              <strong><%= siteName %> — Verification Code</strong>
            </p>
            <p style="margin: 6px 0 0">
              Code: <strong><%= verificationCode %></strong>
            </p>
            <p style="margin: 6px 0 0">
              Expires in: <%= expiresIn || "15 minutes" %>
            </p>
            <% if (verifyUrl) { %>
            <p style="margin: 6px 0 0">
              Verify: <a href="<%= verifyUrl %>"><%= verifyUrl %></a>
            </p>
            <% } %>
            <p style="margin: 6px 0 0">
              Support:
              <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>
            </p>
            <p style="color: #9ca3af; margin-top: 10px; font-size: 12px">
              If you didn't request this, ignore this message.
            </p>
          </div>
        </td>
      </tr>
    </table>
  </body>
</html>
